groupName: acme.cluster.local

certManager:
  namespace: cert-manager
  serviceAccountName: cert-manager

image:
  repository: zmwangi/cert-manager-linode
  tag: latest
  pullPolicy: Always

nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port: 443

resources: {}
